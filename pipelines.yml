resources:
  - name: GetFiles
    type: FileSpec
    configuration:
      sourceArtifactory: MyArtifactory
      aql: items.find({"repo":"docker-local"})
    
pipelines:
  - name: PropertyDemo
    steps:
      - name: ArtifactoryFiles
        type: Bash
        configurations:
          inputResources:
            - name: GetFiles
        execution:
          onExecute:
            - ls
     
      
